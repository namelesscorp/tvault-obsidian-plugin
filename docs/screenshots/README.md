# Screenshots

These images are referenced by the project README.

`panel.png`, `tokens.png`, `container-details.png` and `entropy.png` were rendered
from the plugin's real `styles.css` and DOM, on Obsidian's dark-theme variables —
so they match the live UI closely. Feel free to replace any of them with an actual
capture from Obsidian (keep the same filenames):

| File                    | View                                                        |
| ----------------------- | ----------------------------------------------------------- |
| `panel.png`             | Side panel of a locked share vault (token fields + Unlock). |
| `tokens.png`            | Generated tokens output (per-share Copy + Save to file…).   |
| `container-details.png` | Expanded **Container details** panel.                       |
| `entropy.png`           | The **Gather entropy** modal.                               |

To recapture from the real app: dark theme, crop to the panel/modal, PNG.
